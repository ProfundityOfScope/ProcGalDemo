"""Placement tiers: tier0 (hand-authored mythic Events) and tier1
(procedurally-spawned Great Powers / minor civs / cluster leagues).
Import the specific tier module directly (`.tiers.tier0`/`.tiers.tier1`)
-- which tier a name comes from is meaningful, so it stays visible in
the import path rather than being flattened away by a re-export here."""
